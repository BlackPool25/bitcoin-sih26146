# Assets for offline bundle

Assets for offline bundle: pr_curve, calibration, sigma_sweep.

- `pr_curve.png` - precision recall curve copied from `data/eval/pr_curve.png`
- `calibration.png` - calibration curve, added in Task 15b
- `sigma_sweep.png` - sigma sweep plot, added in Task 15b

These files are bundled for air-gapped review so the model card renders
without network access.
