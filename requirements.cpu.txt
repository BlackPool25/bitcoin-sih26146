# CPU — ML stack for SIH26146 M3 Risk Scoring (air-gapped / portable)
# Install: pip install -r requirements.cpu.txt
# CPU wheels via PyTorch CPU index; PyG via CPU wheels

--extra-index-url https://download.pytorch.org/whl/cpu

# Core CPU torch
torch==2.4.1+cpu

# PyG + extensions — CPU wheels (via PyTorch CPU index)
pyg_lib
torch_scatter
torch_sparse
torch_cluster
torch_spline_conv
torch_geometric>=2.6

# ML / scoring deps
scikit-learn>=1.4
shap>=0.44
jinja2
networkx>=3.3
polars>=1.12
duckdb>=1.1

# Runtime deps (from pyproject.toml)
fastapi>=0.110
uvicorn>=0.30
pydantic>=2.9
geoip2>=4.8
maxminddb>=2.2
python-louvain>=0.16
ijson>=3.3
lxml>=5.0
watchdog>=4.0
python-magic>=0.4.27
faker>=24.0
typer>=0.12
