# Handoffs — Contract Changes (read every turn)

> Append when you change a frozen contract or need another agent's file. Prefix `BREAKING:` if it requires others to react.

| Date | From | To | Change | Action Required |
|------|------|----|--------|-----------------|
| 2026-08-23 | Lead | All | `schema.sql` v1 frozen: nodes(id PK, type ENUM, country, asn, community_id) + edges(src,dst,type,amount,ts,weight) indices — `data/graph/duck.db` | M1 writes Parquet that respects it; M2 owns it; M3 reads it only |
| 2026-08-23 | Lead | All | `openapi.yaml` v1 frozen: GET /api/alerts, /api/graph/{id}, /api/evidence/{id}, /api/geo/{ip}, /api/mock/mempool, /api/replay | M4 proposes, M5 reads, M1/M3 serve |
| 2026-08-23 | Lead | All | Coord protocol activated: every turn must read+write `.coord/*` | All terminals must `cat .coord/progress.md` at start |
| 2026-08-24 | M2 | All | BREAKING: `schema.sql` v1 FROZEN — geo_cache(ip PK, country, city, asn, lat, lng, radius, fetched_at) + nodes(id PK, type ENUM, country, asn, community_id) + edges(src,dst,type,amount,ts,weight) + indices idx_edges_src/dst/ts, idx_nodes_community (+idx_geo_cache_asn) — duckdb :memory: verified | Do not ALTER without handoff |
| (agents append) | — | — | — | — |

| 2026-08-24 | M2 | M3 | M2 DONE — `data/graph/duck.db` schema frozen (nodes/edges/geo_cache +5 indices) 3531 nodes 2627 edges ratio 0.0008 — M3 may read duck.db + nodes/edges.parquet only, do not mutate | M3 reads `data/graph/duck.db` via duckdb, respects community_id |
| 2026-08-24 | M2 | All | No BREAKING — schema unchanged, build CLI stable `--input <glob> --out data/graph/ [--duckdb path]` | — |
| 2026-08-24 | M1 | M2 | data/clean/parquet/synth_50k.parquet ready 50000 rows TransactionRecord 14 cols — M2 build.py input | M2 reads parquet/*.parquet per schema.sql v1 |
| 2026-08-24 | M1 | M4,M5,M6 | openapi.yaml v1 extended: POST /api/ingest, /api/ingest/batch, /api/ingest/status/{id}, /api/validation/{file}, /api/mock/mempool, WS /ws/mock/mempool, /api/replay?at, /api/ingest/watch/* — 9 paths validator OK | Lead merge done |
| 2026-08-24 | M5 | All | M5 mock-first vs stub 2026-08-24: decision mock-first via frontend handlers (`frontend/src/mocks/handlers.ts` + fixtures graph-80/2000 replay-1000 geo-centroids) for Wave3 TDD; optional read-only backend/api/graph.py+geo.py stub gated behind feature flag if integration needs real HTTP; no openapi.yaml mutation, no schema.sql mutation, respects M1 Owns; pagination truncateByLimit 2000 enforced (`types/graph.ts`), 422 on missing/invalid `at` (`types/replay.ts` + handlers), Z and +05:30 parsed via parseISOWithTZ, radius hint only never WHERE filter (`types/geo.ts`) | Wave3 use `mockFetch` / `installMockFetch` and `toCyJsonFromRows` contracts; do not render >2000 without limit |
| 2026-08-24 | M6 | All | BREAKING: M6 DONE — data/eval/* schema pr.json/stress.json/sigma_sweep.json/fidelity.json frozen, docker-compose.yml draft ready for Lead digest review, model_card.md metrics filled | M3/M4/M5 read data/eval, Lead reviews compose |
| 2026-08-24 | M6 | M3 | duck.db 5.8M ready for ML — M3 reads data/graph/duck.db+nodes/edges.parquet, respects community_id, uses TORCH_BLAS_PREFER_HIPBLASLT=0 for gfx1100 | M3 ml/train_gnn.py |
| 2026-08-24 | M6 | M4/M5 | GET /api/alerts graph shape ready after M3 — use data/eval/pr.json 0.51 stress 0.615 for viz thresholds, docs/assets 4 pngs for writeup | M4/M5 AlertTable GraphView |
| 2026-08-24 | M5 | All | M5 selectedAlertId contract 2026-08-24: App owns selectedId state, passes to GraphView, fetches GET /api/graph/{id}?limit=2000, M4 owns alert table not queried directly, no mutation | App fetches graph via limit=2000, ReplaySlider lifts via toCyJsonFromRows, no schema.sql/openapi.yaml mutation, no city truth |
| 2026-08-24 | M4 | All | BREAKING: openapi.yaml v1 Alert+Evidence schemas — GET /api/alerts query limit 1-1000 default50 offset sort[p,-p,rank] tier[critical,high,medium,low] q string response {alerts:Alert[],count,limit,offset} + GET /api/evidence/{id} 200 Evidence 404 Error — components/schemas Alert(required alert_id/rank/p/tier, txid pattern ^[a-f0-9]{64}$, p 0-1, geo_country, timestamp) Evidence(shap Record, top_shap string[3], nl, geo_timeline, amount_flow, temporal_burst, accuracy_hint radius_km is_hint) — preserves TransactionRecord + 14 paths validator OK | M5 reads Evidence shape for GraphView, M3 serves ranked.parquet via /api/alerts, M6 bundles openapi |
| 2026-08-24 | M4 | M5 | M4 DONE — AlertTable rang 1-50 p 0.95→0.52 tier badges Crit red High orange Med amber Low slate why top SHAP geo flags time sorted p desc filter tier/search pagination 50 onSelectAlert → App selectedAlertId → EvidencePanel + GraphView via /api/graph/{id}?limit=2000 limit guard — App owns 1.7fr/1fr 1280px dark investigator theme, no /api/graph fetch in M4 owns, EvidencePanel SHAP waterfall + timelines + accuracy_hint badge | M5 verifies GraphView preset <2K not broken, reads App selectedAlertId contract |
| 2026-08-24 | M4 | M3 | M4 fallback to mockAlerts/mockEvidence when GET /api/alerts 404 — M3 to serve ranked.parquet via GET /api/alerts when ready, fallback will auto-switch | M3 implements backend/api/alerts.py to serve data/alerts/ranked.parquet |
| 2026-08-24 | M5 | All | M5 DONE — no API shape changes, no schema.sql/openapi.yaml/backend/api/ingest.py mutation in M5 turn (git diff forbidden clean for M5 scope; openapi.yaml dirty from M1-M4 pre-existing not M5). Files owned: frontend/src/components/GraphView.tsx (preset <2K, sigma hatch dynamic import + fcose fallback), GeoMap.tsx (leaflet tileLayer+markers+fallback canvas, radius hint only), ReplaySlider.tsx (debounce 180ms abort, 422 Missing at, Z/+05:30 parseISOWithTZ), App.tsx selectedAlertId contract limit=2000, cytoscape/styles+layout+update+pagination+toGraphology, leaflet/markers+tileLayer+fallback, bench/report.json passed true, playwright tests/e2e/m5.spec.ts + e2e/m5.spec.ts 8 specs, vitest src/__tests__/m5.contract.test.tsx 13 tests. Contract: App owns selectedAlertId, fetches GET /api/graph/{id}?limit=2000, M4 owns alerts via AlertTable prop onSelectAlert, sigma hatch manual toggle ?renderer=sigma -> sigma-view, graph empty state 404 -> no subgraph | M3/M4 consume GraphView/GeoMap/ReplaySlider via App prop, no schema mutation |
